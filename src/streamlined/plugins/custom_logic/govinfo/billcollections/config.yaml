- name: billcollections
  description: GovInfo Bill Collections main data

  api:
    api_endpoint: BILLS
    page_size: 1000
    retry_attempts: 3

  schema:
    table_name: billcollections

    is_main: true
    create_raw: true
    id_fields: [package_id]


  processing:
    batch_size: 100
    checkpoint_frequency: 50
    scaling_factor: 50
    has_granules: false