- name: congressional_directories
  description: GovInfo Congressional Directories main data

  api:
    collection_code: CDIR
    page_size: 1000
    retry_attempts: 3

  schema:
    table_name: congressional_directories
    is_main: true
    create_raw: true
    id_fields: [package_id]
    related_tables: []

  processing:
    batch_size: 100
    checkpoint_frequency: 50
    has_granules: true
    granule_name: members

- name: members
  description: Congressional Directory Members granule data

  api:
    page_size: 1000
    retry_attempts: 3

  schema:
    table_name: members
    is_main: false
    create_raw: true
    id_fields: [granule_id]

  processing:
    batch_size: 100